const areas = ["Food Service", "Retail / GGCC", "MMPP / Trimmings", "Ventas Nacionales", "Maquila / desarrollo", "Marca propia", "Otro"];

export function ContactPreview() {
  return <section id="contacto" className="relative scroll-mt-24 overflow-hidden bg-blue-50 px-6 py-20 sm:px-8 lg:py-28"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[0.7fr_1.3fr]"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-700">Contacto</p><h2 className="mt-4 font-display text-5xl text-navy">¿Qué necesita tu negocio?</h2><p className="mt-5 max-w-sm leading-7 text-ink/70">Cuéntanos tu necesidad y te derivamos al equipo indicado.</p></div><form className="grid gap-5 sm:grid-cols-2" aria-label="Formulario disponible en el siguiente hito">{["Nombre", "Empresa", "Email", "Teléfono"].map((field) => <label key={field} className="text-sm font-bold text-navy">{field}<input disabled className="mt-2 w-full rounded-lg border border-navy/10 bg-white px-4 py-3" /></label>)}<fieldset className="sm:col-span-2"><legend className="text-sm font-bold text-navy">¿En qué área te podemos ayudar?</legend><div className="mt-3 flex flex-wrap gap-2">{areas.map((area) => <span key={area} className="rounded-full bg-white px-4 py-2 text-xs font-semibold text-navy">{area}</span>)}</div></fieldset><button disabled className="rounded-full bg-navy px-7 py-3.5 text-sm font-bold text-white opacity-60 sm:col-span-2">Conversemos</button></form></div></section>;
}
