import Image from "next/image";
import type { PublicRepresentative } from "@/lib/agenda/types";
import { normalizeWhatsAppNumber } from "@/lib/agenda/whatsapp";

export function CommercialTeam({ configured, representatives }: { configured: boolean; representatives: PublicRepresentative[] }) {
  return <section id="equipo" className="scroll-mt-24 bg-gray-50 px-6 py-20 sm:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="text-center"><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-700">Nuestro equipo comercial</p><h2 className="mt-3 font-display text-4xl text-navy sm:text-5xl">Habla con quien entiende tu negocio.</h2></div><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{representatives.slice(0, 4).map((rep) => { const number = normalizeWhatsAppNumber(rep.whatsapp); return <article key={rep.id} className="rounded-2xl bg-white p-5 shadow-sm"><div className="relative aspect-[4/3] overflow-hidden rounded-xl bg-blue-50">{rep.photoUrl ? <Image src={rep.photoUrl} alt={`Retrato de ${rep.name}`} fill sizes="(min-width:1024px) 25vw, 50vw" className="object-cover" /> : <span className="grid h-full place-items-center font-display text-4xl text-navy/50">{rep.name.split(" ").map((part) => part[0]).slice(0, 2).join("")}</span>}</div><h3 className="mt-5 font-display text-xl text-navy">{rep.name}</h3><p className="mt-1 text-sm text-ink/65">{rep.areaLabel}</p><div className="mt-4 flex gap-4 text-sm font-bold text-blue-700">{number && <a href={`https://wa.me/${number}`} target="_blank" rel="noreferrer">WhatsApp</a>}{rep.contactEmail && <a href={`mailto:${rep.contactEmail}`}>Email</a>}{!number && !rep.contactEmail && <span>Contacto por confirmar</span>}</div>{!configured && <p className="mt-3 text-xs font-semibold text-navy/45">Perfil temporal</p>}</article>; })}</div></div></section>;
}
